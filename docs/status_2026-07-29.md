# Status — 29 July 2026

Supersedes the status fields in `PENG_DELIVERABLES.md`. Covers: correctness/robustness
audit of the existing work, what now exists against Peng's four asks, and next steps.

---

## 0. Correctness & robustness audit (done first)

**Confirmed sound.**
- The additive location surrogate (`h_base + g_lat + g_lon`) is *exact* for the model
  class used (per-column splines + Ridge has no interactions), not an approximation.
  Stated explicitly in the consolidated draft.
- Per-scale minimum-signal rule, Bonferroni over dipped scales, S1/S2/S3 budget splits,
  minimum-mass rule, and the π_true ground-truth correction are implemented as described
  in `RESULTS_2026-07-28.md`.
- Independent re-verification: rebuilding the pipeline from the raw CSV reproduces the
  published anchors exactly — gated share 0.077, honest R² 0.338 — and a 40-building
  slice of the main audit reproduces the published behaviour (flags boundary-adjacent
  in b, median Δ̂ 0.298 vs published 0.291–0.298, zero honest flags, honest placebo
  Δ̂ 0.017).

**Problems found (and their status).**
1. `exp_seattle_v3.py` docstring said 6 scales; code has 3 (`geomspace(25,1200,3)`,
   m=1000 — the §6e allocation). The §6d table ("median 4 of 6 scales") came from an
   earlier state of the code, and the `v3_*.csv` outputs behind §6d/6e were never saved.
   → The controlled simulation now independently reproduces the §6d/6e phenomena; the
   Seattle full rerun under `run_all.sh` will regenerate the CSVs. Flag for the draft:
   quote §6d numbers as "protocol as of 28 July," pending the rerun.
2. Three files hardcoded a dead sandbox path (`/sessions/eager-intelligent-noether/…`);
   nothing was rerunnable. → Fixed in the repo copies via `paths.py` (env-var override +
   repo-relative defaults). Vault copies left untouched as the historical record.
3. `seattle_buildings.csv` / `seattle_prep.npz` / `seattle_model.pkl` were built ad hoc
   and lost — a silent reproducibility hole. → `prep_seattle.py` now builds all three
   from raw data (resumable; bbox-accelerated raster).
4. Minor: RESULTS says "1200² raster," code uses 1600² (code authoritative; noted in
   `configs/routing_audit.yaml`). Honest/gated runs share an RNG stream (not
   probe-paired; statistically fine). `~$ROMANI_ROBERT.pptx` lock file — PowerPoint has
   the poster open somewhere.
5. New robustness finding (from the simulation): dipping at scales **above the honest
   model's lengthscale** produces false positives (0.33–0.81 on ℓ-violating GP nulls vs
   0.00–0.04 when ℓ exceeds the ladder top). Seattle was safe (2367 m vs 1200 m), but
   this is now assumption **A11** with an operational rule: cap the ladder below the
   honest lengthscale. Conversely, a *continuous* kink is NOT flagged (0.05 = nominal) —
   the dip is specific to jumps, a sharper specificity than v2 assumed.

---

## 1. Peng's four asks — where each stands

### (1) Consolidated draft — DONE (draft ready for Peng)
`consolidated_draft_v3.tex/pdf` (8 pp), in the vault and `docs/routing_audit/`.
In Peng's order: regime definition + identifiability ceiling + boundary estimand;
inputs/outputs; assumptions B1–B10 + new A11 (honest-model smoothness, verified sharp)
and A12 (covariate smoothness for on-manifold probes); complete algorithm (Stage A′
naive dip scan, Stage B′ K=2 recovery with Δ̂ at the p_min scale, Stage C′
randomization/BY/conformal); what changed from v2 with the evidence; the new simulation;
the baseline comparison. Describes the *current* method, not v2.

### (2) Simulation with known regimes — DONE (run, not just written)
`sim/` (vault + repo): 1-D and 2-D constructions, ground truth known by construction.
- **Noise axis** (τ_obs 0.005→0.2, Δ/τ 60×→1.5×): power 0.98→0.89→0.57→~0;
  FP ≤ 0.004 everywhere; the honest model **abstains** (0.79) rather than flags at
  extreme noise; Δ̂ stays 0.248–0.288 vs true 0.30 wherever flags exist.
- **Boundary axis**: power 1.00 for π_true>0.10, 0.55–0.71 at 0.05–0.10, 0 below —
  minor-mode mass m·π is the binding constraint, reproducing the Seattle finding in a
  controlled setting. Detection radius = probe reach (2-D: 1.00 inside the ladder top,
  0.015 beyond).
- **2-D**: power 0.929, FP 0.000, orientation 0.956, flags trace the wavy boundary.
- Self-contained (numpy/pandas/matplotlib; MC-calibrated dip fallback when `diptest`
  isn't installed), seeded, resumable. Figures: `fig_sim_known_regimes.png`,
  `fig_sim2d_map.png`.

### (3) Comparison vs graph-based approach + baselines — DONE
`sim/baseline_comparison.csv`; SBM run with the actual `src/sbm` code (plain SBM K=2,
kNN cosine graph over behavioural fingerprints):

| method | existence FP | partition acc (all/detectable) | Δ̂ err | queries/anchor |
|---|---|---|---|---|
| dip-scan (ours) | 0.000 | 0.80 / 0.93 | 0.03 | 6001 |
| global cluster+BIC | 0.00 | 0.54 / 0.54 | 0.30 | 25 |
| residual outlier | 0.008 | 0.52 / 0.52 | — | 25 |
| SPA profile-mean | no test | 0.52 / 0.58 | 0.21 | 50 |
| plain SBM K=2 (their code) | no test | 0.61 / — | — | 33 |

Notable: global clustering *does* answer existence here (BIC K=2 gated / K=1 honest)
but partitions at chance; SBM aligns weakly (ARI 0.048) because fingerprint similarity
cancels level effects except across boundaries — intrinsic, not tuning.

### (4) Reproducible GitHub repo — STAGED, needs your commit
Working-tree changes in `geospatial-xai-attacks` (nothing committed; review then commit):
- `scripts/routing_audit/` — all 27 scripts (dead paths fixed via `paths.py`), new
  `prep_seattle.py`, `run_all.sh` (canonical order, block-parallel, resumable), `sim/`.
- `configs/routing_audit.yaml` — every registered setting + seeds.
- `docs/routing_audit/` — consolidated draft (tex+pdf) + RESULTS log.
- `results/routing_audit/figures/` — the six current-protocol figures.
- `README.md` — new "current thread" section on top; April SBM narrative marked as
  history. `.gitignore` (raw CSV + intermediates), `requirements.txt` (+diptest),
  `data/README_routing_audit.md` (download instructions).
- Verified end-to-end in a clean environment (see §0).
- Raw CSV copied to `data/raw/` (git-ignored).

Suggested commit message:
`Routing-audit thread: consolidated method note v3, known-regimes simulation + baselines (incl. SBM), reproducible pipeline (prep + run_all + configs)`

---

## 2. Next steps

**Code.** (a) Full Seattle rerun via `run_all.sh` to regenerate the §6d/6e CSVs under
the committed protocol (hours; block-parallel). (b) Report the Δ̂ filter as a
precision/recall curve (one plotting script; data already exists). (c) Optional: adopt
`Δ̂ at p_min scale` in `exp_seattle_v3.py` (the sim shows it dominates the across-scale
median).

**Theory.** (a) General-K synchronization + certification with the v2 §13 theorem
target — the largest open item; K=2 local recovery is solved and documented. (b) A
formal statement of A11 (FP rate as a function of ℓ/σ_T and amplitude/Δ — the sim gives
the empirical curve to match). (c) Δ̂ small-π bias correction, plausibly via pooling
across the anchor graph — same machinery as (a).

**Experiment design.** (a) Generalization beyond geospatial: discrete anchor graphs
(feeder/rate class/tariff) sidestep A12 entirely — data acquisition is the blocker.
(b) Proper residualization for A12-violating covariates (current partial fix: honest FP
0.27→0.10, still 2× nominal). (c) Adaptive-adversary suite (response smoothing near
probe scales) — scoped in v2, untouched.

**For the Peng meeting.** The four deliverables stand alone: draft (1), simulation with
figures (2), comparison table (3), repo (4, after your commit). Open-question list to
bring: general-K priority vs generalization-beyond-geospatial; whether A11 should be a
declared assumption or an estimated quantity in deployments where the honest model is
unavailable; target venue for the consolidated note.
